# Configuração completa do kube-prometheus-stack
fullnameOverride: "prometheus"

# Alertmanager
alertmanager:
  enabled: true
  
  # Ingress do Alertmanager
  ingress:
    enabled: true
    ingressClassName: "nginx"
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
    hosts:
      - alertmanager.devopsr66.com
    paths: /
    tls:
      - secretName: "devopsr66-tls"
        hosts:
          - alertmanager.devopsr66.com
  
  # Configuração do Alertmanager
  config:
    global:
      resolve_timeout: 5m
    
    route:
      group_by: ['alertname', 'cluster', 'service']
      group_wait: 10s
      group_interval: 10s
      repeat_interval: 12h
      receiver: 'default'
    
    receivers:
      - name: 'default'
        slack_configs:
          - channel: '#prometheus'
            send_resolved: true
            api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK'
            title: '{{ .GroupLabels.alertname }}'
            text: |
              *Alert:* {{ .CommonLabels.alertname }}
              *Severity:* {{ .CommonLabels.severity }}
              *Instance:* {{ .CommonLabels.instance }}
              *Description:* {{ .CommonAnnotations.description }}
        telegram_configs:
          - bot_token: 'YOUR_BOT_TOKEN'
            chat_id: YOUR_CHAT_ID
            send_resolved: true
            message: |
              🚨 *Alerta:* {{ .CommonLabels.alertname }}
              *Gravidade:* {{ .CommonLabels.severity }}
              *Instância:* {{ .CommonLabels.instance }}
              *Descrição:* {{ .CommonAnnotations.description }}
  
  # Storage do Alertmanager
  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: nfs-storage
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 2Gi

# Grafana
grafana:
  enabled: true
  
  adminPassword: admin  # ⚠️ MUDE ISSO!
  
  # Ingress do Grafana
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
    hosts:
      - grafana.devopsr66.com
    path: /
    tls:
      - secretName: devopsr66-tls
        hosts:
          - grafana.devopsr66.com
  
  # Storage do Grafana
  persistence:
    enabled: true
    storageClassName: nfs-storage
    size: 2Gi
  
  # Datasources (automático)
  sidecar:
    dashboards:
      enabled: true
    datasources:
      enabled: true

# Prometheus
prometheus:
  enabled: true
  
  # Ingress do Prometheus
  ingress:
    enabled: true
    ingressClassName: "nginx"
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
    hosts:
      - prometheus.devopsr66.com
    paths:
      - /
    tls:
      - secretName: "devopsr66-tls"
        hosts:
          - prometheus.devopsr66.com
  
  # Configurações do Prometheus
  prometheusSpec:
    # Retenção
    retention: 10d
    
    # Storage
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: nfs-storage
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 8Gi
    
    # Recursos
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: 1000m
        memory: 2Gi
    
    # Service Monitors (descoberta automática)
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false
    ruleSelectorNilUsesHelmValues: false

# Node Exporter
nodeExporter:
  enabled: true

# Kube State Metrics
kubeStateMetrics:
  enabled: true

# Prometheus Operator
prometheusOperator:
  enabled: true

# Componentes do Kubernetes
kubeApiServer:
  enabled: true

kubelet:
  enabled: true

kubeControllerManager:
  enabled: true

kubeScheduler:
  enabled: true

kubeProxy:
  enabled: true

coreDns:
  enabled: true

# Regras de alerta padrão
defaultRules:
  create: true
  rules:
    alertmanager: true
    etcd: false
    general: true
    k8s: true
    kubelet: true
    kubeProxy: true
    kubernetes: true
    node: true
    prometheus: true