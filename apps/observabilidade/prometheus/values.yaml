# Configuração global
fullnameOverride: prometheus

# Prometheus Operator
prometheusOperator:
  enabled: true
  
# Prometheus Instance
prometheus:
  enabled: true
  
  prometheusSpec:
    # Retenção de dados
    retention: 10d
    retentionSize: ""
    
    # Recursos
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: 1000m
        memory: 2Gi
    
    # Storage
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: nfs-storage
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 8Gi
    
    # External labels
    externalLabels:
      cluster: kubernetes
      environment: production
    
    # Service Monitors
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false
    ruleSelectorNilUsesHelmValues: false
  
  # Ingress para Prometheus
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
    hosts:
      - prometheus.devopsr66.com
    paths:
      - /
    pathType: Prefix
    tls:
      - secretName: devopsr66-tls
        hosts:
          - prometheus.devopsr66.com

# Alertmanager
alertmanager:
  enabled: true
  
  alertmanagerSpec:
    # Storage
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: nfs-storage
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 2Gi
    
    # Recursos
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi
  
  # Configuração do Alertmanager
  config:
    global:
      resolve_timeout: 5m
    
    route:
      group_by: ['alertname', 'cluster', 'service']
      group_wait: 10s
      group_interval: 10s
      repeat_interval: 12h
      receiver: 'default'
      routes:
        - receiver: 'default'
          matchers:
            - alertname =~ ".*"
    
    receivers:
      - name: 'default'
        slack_configs:
          - channel: '#prometheus'
            send_resolved: true
            api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK'
            title: '{{ .GroupLabels.alertname }}'
            text: |
              *Alert:* {{ .CommonLabels.alertname }}
              *Severity:* {{ .CommonLabels.severity }}
              *Instance:* {{ .CommonLabels.instance }}
              *Description:* {{ .CommonAnnotations.description }}
              *Status:* {{ .Status }}
        telegram_configs:
          - bot_token: 'YOUR_BOT_TOKEN'
            chat_id: YOUR_CHAT_ID
            send_resolved: true
            message: |
              🚨 *Alerta:* {{ .CommonLabels.alertname }}
              *Gravidade:* {{ .CommonLabels.severity }}
              *Instância:* {{ .CommonLabels.instance }}
              *Descrição:* {{ .CommonAnnotations.description }}
              *Status:* {{ .Status }}
  
  # Ingress para Alertmanager
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
    hosts:
      - alertmanager.devopsr66.com
    paths:
      - /
    pathType: Prefix
    tls:
      - secretName: devopsr66-tls
        hosts:
          - alertmanager.devopsr66.com

# Grafana
grafana:
  enabled: true
  
  adminPassword: admin  # ⚠️ MUDE ISSO!
  
  # Ingress para Grafana
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
    hosts:
      - grafana.devopsr66.com
    path: /
    pathType: Prefix
    tls:
      - secretName: devopsr66-tls
        hosts:
          - grafana.devopsr66.com
  
  # Persistence
  persistence:
    enabled: true
    storageClassName: nfs-storage
    size: 2Gi
  
  # Datasources (Prometheus já vem configurado automaticamente)
  sidecar:
    dashboards:
      enabled: true
      defaultFolderName: "General"
    datasources:
      enabled: true
      defaultDatasourceEnabled: true
  
  # Recursos
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

# Node Exporter (coleta métricas dos nodes)
nodeExporter:
  enabled: true

# Kube State Metrics (métricas do Kubernetes)
kubeStateMetrics:
  enabled: true

# Configurações de descoberta de componentes do Kubernetes
kubeApiServer:
  enabled: true

kubelet:
  enabled: true
  namespace: kube-system

kubeControllerManager:
  enabled: true
  endpoints: []

kubeScheduler:
  enabled: true
  endpoints: []

kubeProxy:
  enabled: true
  endpoints: []

kubeEtcd:
  enabled: false  # Geralmente não exposto

coreDns:
  enabled: true

# Default rules (alertas prontos)
defaultRules:
  create: true
  rules:
    alertmanager: true
    etcd: false
    configReloaders: true
    general: true
    k8sContainerCpuUsageSecondsTotal: true
    k8sContainerMemoryCache: true
    k8sContainerMemoryRss: true
    k8sContainerMemorySwap: true
    k8sContainerResource: true
    k8sContainerMemoryWorkingSetBytes: true
    k8sPodOwner: true
    kubeApiserverAvailability: true
    kubeApiserverBurnrate: true
    kubeApiserverHistogram: true
    kubeApiserverSlos: true
    kubeControllerManager: true
    kubelet: true
    kubeProxy: true
    kubePrometheusGeneral: true
    kubePrometheusNodeRecording: true
    kubernetesApps: true
    kubernetesResources: true
    kubernetesStorage: true
    kubernetesSystem: true
    kubeSchedulerAlerting: true
    kubeSchedulerRecording: true
    kubeStateMetrics: true
    network: true
    node: true
    nodeExporterAlerting: true
    nodeExporterRecording: true
    prometheus: true
    prometheusOperator: true